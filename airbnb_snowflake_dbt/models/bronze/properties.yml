models:
  - name: bronze_bookings
    config:
      materialized: view
  - name: bronze_listings
    config:
      materialized: table
  - name: bronze_hosts
    config:
      materialized: table
        